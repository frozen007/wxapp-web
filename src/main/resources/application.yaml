spring:
  profiles:
    active: prod

  application:
    name: wxapp-web


eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka.zmy-inc.com:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

