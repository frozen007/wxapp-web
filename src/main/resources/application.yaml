spring:
  profiles:
    active: dev

  application:
    name: springdemo


eureka:
  instance:

  client:
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

